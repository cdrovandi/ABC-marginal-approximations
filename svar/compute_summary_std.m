function S = compute_summary_std(Y,extra_args)

% code to compute the standard deviation summary in the sparse VAR example
% that is designed to be informative about the noise parameter

S = std(Y(:));


end